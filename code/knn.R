# Load the common stuff
source("code/common.R")

#############################
# Perform k-Nearest Neighbour
#############################
performkNN <- function(sitenum){
  site.data <- handleMissingData(volume.data[,1])
  
}
performkNN(1)